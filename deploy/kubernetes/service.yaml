# ClusterIP Service：稳定服务发现端点（替代硬编码 localhost，反模式 #5）
apiVersion: v1
kind: Service
metadata:
  name: seedvr2
  labels:
    app: seedvr2
spec:
  type: ClusterIP
  selector:
    app: seedvr2
  ports:
    - name: http
      port: 80
      targetPort: http
