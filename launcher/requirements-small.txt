# ============================================================
# 便携包非 torch 依赖清单（build_portable_bundle.ps1 预装进 WinPython）
#
# 钉版策略（发布管理体系评估 P1-3）：全部 == 精确钉版，版本与
# requirements-lock.txt（venv 实测环境）对齐，保证两次构建产出的
# 便携解释器依赖完全一致。升级依赖时先在开发环境验证，再同步
# requirements.txt / requirements-lock.txt / 本文件三处版本。
# ============================================================

# Core
fastapi==0.141.1
uvicorn[standard]==0.52.4
gunicorn==26.1.0
pydantic==2.13.4
pyyaml==6.0.3

# Template & Frontend
jinja2==3.1.6
python-multipart==0.0.32

# Deep Learning

# Model & Inference
transformers==5.15.0
safetensors==0.8.0
huggingface_hub==1.28.0
einops==0.8.2
omegaconf==2.3.1
diffusers==0.39.0
mediapy==1.2.7
rotary-embedding-torch==0.9.1

# Video Processing
opencv-python==5.0.0.93
imageio==2.37.4
imageio-ffmpeg==0.6.0
pillow==12.3.0

# Database
aiosqlite==0.22.1

# Utilities
aiofiles==25.1.0
psutil==7.2.2
rich==15.0.0
PyWavelets==1.9.0

# Flash Attention (需要 CUDA 编译环境，Windows 下可能需要预编译包)
# 如果安装失败，请从以下地址下载预编译 whl：
# https://github.com/Dao-AILab/flash-attention/releases
# flash-attn>=2.5.0

# Apex (需要 CUDA 编译环境，Windows 下可能需要预编译包)
# 如果安装失败，请从 HuggingFace 下载预编译 whl：
# https://huggingface.co/ByteDance-Seed/SeedVR2-3B/tree/main
# apex

# 注意：flash_attn 和 apex 在 Windows 上的安装可能需要：
# 1. 安装 Visual Studio Build Tools
# 2. 安装 CUDA Toolkit
# 3. 使用预编译的 whl 文件
# 如果无法安装，应用将自动回退到标准注意力机制
